version: '3.8'

services:
  nextjs:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=production
    volumes:
      # 개발 모드에서 사용할 경우 (선택사항)
      # - .:/app
      # - /app/node_modules
      # - /app/.next
    restart: unless-stopped
